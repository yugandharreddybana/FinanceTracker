# Nixpacks configuration for a Java + Node.js monorepo
[phases.setup]
nixPkgs = ["nodejs_22", "jdk17", "maven"]

[phases.install]
cmds = ["npm install", "cd backend && mvn clean install -DskipTests"]

[phases.build]
cmds = ["npm run build:frontend"]

[start]
cmd = "npm run start:full"

