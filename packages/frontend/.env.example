# Vite exposes only vars prefixed with VITE_ — never put API keys here.
# AI keys (GEMINI_API_KEY, NVIDIA_API_KEY, etc.) belong in the Node server env on Railway — see repo root `.env.example`.

VITE_MIDDLEWARE_URL=http://localhost:4000
VITE_AGENT_DEBUG=false
